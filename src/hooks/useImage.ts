
interface Props {

}

// 打开原图
export const openUrl = (imageList: [Props], index: number) => {

}

// 打开授权文件
export const openLicense = () => {

}
// 显示图片详情
export const showDetails = () => {

}

// 显示图片操作日志
export const showLogList = () => {

}

// 显示中图
export const showMiddleImage = () => {

}

// 设置不通过原因
export const setReject = async (imageList: [Props], index: number): Promise<[Props]> => {
  return new Promise((resole, reject) => {

  })
}

// 设置通过
export const setResolve = async (imageList: [Props], index: number): Promise<[Props]> => {
  return new Promise((resole, reject) => {

  })
}
